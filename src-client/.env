# Database configuration for Docker and Backend
DATABASE_URL=postgres://gbilly_sysadmin:BillyJinn228@localhost:5433/Voice-ka_Local

# Docker configuration
POSTGRES_USER=gbilly_sysadmin
POSTGRES_PASSWORD=BillyJinn228
POSTGRES_DB=Voice-ka_Local

# Optional: Skip data clearing (set to 'true' to keep existing data)
# SKIP_DB_CLEAR=true

# Optional: Skip seeding test data (set to 'true' to skip)
# SKIP_DB_SEED=true